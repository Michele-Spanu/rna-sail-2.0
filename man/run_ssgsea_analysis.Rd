% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssgsea_analysis.R
\name{run_ssgsea_analysis}
\alias{run_ssgsea_analysis}
\title{Run ssGSEA analysis}
\usage{
run_ssgsea_analysis(
  expr_data,
  metadata,
  condition_column,
  sample_id_column,
  group1,
  group2,
  species = "mouse",
  extra_pathways = NULL,
  min_size = 15,
  max_size = 500,
  output_dir,
  experiment_name,
  n_boxplot_pathways = 20
)
}
\arguments{
\item{expr_data}{Expression matrix (genes x samples), typically log-TPM}

\item{metadata}{Sample metadata data.frame}

\item{condition_column}{Column in metadata with group labels (e.g. "condition")}

\item{sample_id_column}{Column in metadata with sample IDs matching colnames(expr_data)}

\item{group1}{Name of group 1 (e.g. "mut")}

\item{group2}{Name of group 2 (e.g. "wt")}

\item{species}{"mouse" or "human"}

\item{extra_pathways}{Optional named list of extra pathways
(names = pathway names, values = vectors of Ensembl IDs).
These are added on top of the Hallmark sets.}

\item{min_size}{Minimum gene set size (default: 15)}

\item{max_size}{Maximum gene set size (default: 500)}

\item{output_dir}{Output directory}

\item{experiment_name}{Experiment name (used for file names)}

\item{n_boxplot_pathways}{Number of pathways to include in boxplot (top by FDR, default: 20)}
}
\value{
List with:
  \item{scores}{matrix of ssGSEA scores (pathways x samples)}
  \item{stats}{data.frame with per-pathway Wilcoxon test results}
  \item{pathways}{list of pathways used}
}
\description{
Performs single-sample GSEA (ssGSEA) using GSVA on Hallmark gene sets
from MSigDB by default, with the option to add custom pathways.
}
